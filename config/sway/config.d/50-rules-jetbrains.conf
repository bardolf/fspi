# JetBrains IDE window rules.
# focus_follows_mouse is toggled per-app by scripts/sway-ffm-jetbrains.sh
# (sway has no per-criteria FFM). Don't set it statically here — every
# reload would re-apply it and stomp the watcher's runtime value.

# Splash screen - float, center, don't steal focus
for_window [app_id="jetbrains-.*" title="splash"] {
    floating enable
    no_focus
    border none
    move position center
}

# Tooltips and autocomplete popups (title starts with "win") - don't steal focus
for_window [app_id="jetbrains-.*" title="^win"] {
    no_focus
}
