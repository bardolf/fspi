# Per-monitor scaling configuration
# Order matters: wildcards first, then specific monitors override

# Default for all monitors (e.g., 1080p notebook displays)
# No scaling needed for standard 1080p
output * scale 1

# Desktop 4K monitor (Dell P2723QE) - override with scaling
output "Dell Inc. DELL P2723QE 519SYV3" scale 1.3
